#include "../inc/ssl.h"

char        *digest(char *message, int type)
{
    char *md;

    
}

void        md5(t_container container)
{
    /*
    char *md;

    while (container.message)
    {
        md = digest(container.message->content,
                    container.message->content_size);
        container.message = container.message->next;
    }
    */
   printf("file: %d\n", (int)a0);
}